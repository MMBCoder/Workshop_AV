# secrets
.env

# uv / venv
.venv/
# uv.lock IS committed — it pins exact versions so everyone (and Render) installs
# the same thing. `uv sync --frozen` in render.yaml depends on it being here.

# python
__pycache__/
*.pyc

# notebooks
.ipynb_checkpoints/

# local vector stores / data caches created by the notebooks
**/chroma_db/
**/*.sqlite3
